export interface Options {
  templateDir?: string
  exportDir?: string
  languages?: string[]
  /**
   * @default true
   * @description Whether to sort the keys of the object
   */
  sortObjectKeys?: boolean
}
